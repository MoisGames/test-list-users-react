import './App.scss';
import { Routes, Route, Link } from 'react-router-dom';
import SideBar from './components/SideBar'

function App() {
  return (

    <div className="App">
      <SideBar />
      <span>
        APP
      </span>
      <Routes>
      
      </Routes>
    </div>
  );
}

export default App;
